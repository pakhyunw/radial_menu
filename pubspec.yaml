name: radial_menu
description: A flutter radial menu opening and revealing icons in a circle
version: 0.0.1
homepage: https://github.com/OwnWeb/radial_menu

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.11.1
